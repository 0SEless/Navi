Commit: 78a9e73
Imported from root commit (initial project commit)

commit 78a9e738e37faa132c8fab7d32b21d87a3577bc1
Author: jepersonsalaver <jepersonsalaver@gmail.com>
Date:   Sun Jul 5 21:44:07 2026 -0800

    scaffold VoiceCode task 1: VoiceTranscriber module
    
    - tools/voicecode/__init__.py (package marker)
    - tools/voicecode/transcriber.py (VoiceTranscriber class with pynput-based hotkey, faster-whisper transcription, pystray icon)
    - tools/voicecode/requirements.txt (faster-whisper, pyaudio, keyboard, pystray, Pillow)

 .gitignore                       | 11 +++++++
 navi-next                        |  1 +
 tools/voicecode/__init__.py      |  0
 tools/voicecode/requirements.txt |  5 +++
 tools/voicecode/transcriber.py   | 71 ++++++++++++++++++++++++++++++++++++++++
 5 files changed, 88 insertions(+)

diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..df070b6
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1,11 @@
+∩╗┐__pycache__/
+*.pyc
+*.pyo
+*.pyd
+*.egg-info/
+dist/
+build/
+*.egg
+.env
+venv/
+.venv/
diff --git a/navi-next b/navi-next
new file mode 160000
index 0000000..a0ea860
--- /dev/null
+++ b/navi-next
@@ -0,0 +1 @@
+Subproject commit a0ea8609c95611f496a99b197b1b1dc9e9a11bd9
diff --git a/tools/voicecode/__init__.py b/tools/voicecode/__init__.py
new file mode 100644
index 0000000..e69de29
diff --git a/tools/voicecode/requirements.txt b/tools/voicecode/requirements.txt
new file mode 100644
index 0000000..263d946
--- /dev/null
+++ b/tools/voicecode/requirements.txt
@@ -0,0 +1,5 @@
+faster-whisper>=1.0.0
+pyaudio>=0.2.11
+keyboard>=0.13.5
+pystray>=0.19.4
+Pillow>=10.0.0
diff --git a/tools/voicecode/transcriber.py b/tools/voicecode/transcriber.py
new file mode 100644
index 0000000..3ee0338
--- /dev/null
+++ b/tools/voicecode/transcriber.py
@@ -0,0 +1,71 @@
+"""Audio recording and offline transcription via faster-whisper."""
+import threading
+import pyaudio
+import numpy as np
+from faster_whisper import WhisperModel
+
+
+class VoiceTranscriber:
+    """Captures mic audio and transcribes it using a local Whisper model."""
+
+    def __init__(self, model_size: str = "tiny.en", sample_rate: int = 16000):
+        self.model_size = model_size
+        self.sample_rate = sample_rate
+        self._model: WhisperModel | None = None
+        self._audio_frames: list[bytes] = []
+        self._is_recording = False
+        self._thread: threading.Thread | None = None
+
+    # ΓöÇΓöÇ lazy model load ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+    @property
+    def model(self) -> WhisperModel:
+        if self._model is None:
+            self._model = WhisperModel(self.model_size, device="cpu", compute_type="int8")
+        return self._model
+
+    # ΓöÇΓöÇ public API ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+    def start_recording(self) -> None:
+        """Begin capturing audio from the default microphone."""
+        self._audio_frames = []
+        self._is_recording = True
+        self._thread = threading.Thread(target=self._record_loop, daemon=True)
+        self._thread.start()
+
+    def stop_and_transcribe(self) -> str:
+        """Stop recording, transcribe audio, return the text."""
+        self._is_recording = False
+        if self._thread:
+            self._thread.join(timeout=5.0)
+
+        if not self._audio_frames:
+            return ""
+
+        raw = np.frombuffer(b"".join(self._audio_frames), dtype=np.int16)
+        audio = raw.astype(np.float32) / 32768.0
+
+        segments, _ = self.model.transcribe(audio, language="en")
+        return " ".join(seg.text.strip() for seg in segments).strip()
+
+    # ΓöÇΓöÇ internal ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ
+    def _record_loop(self) -> None:
+        p = pyaudio.PyAudio()
+        try:
+            stream = p.open(
+                format=pyaudio.paInt16,
+                channels=1,
+                rate=self.sample_rate,
+                input=True,
+                frames_per_buffer=1024,
+            )
+            frames_collected = 0
+            max_frames = int(30 * self.sample_rate / 1024)  # 30-second cap
+
+            while self._is_recording and frames_collected < max_frames:
+                data = stream.read(1024, exception_on_overflow=False)
+                self._audio_frames.append(data)
+                frames_collected += 1
+
+            stream.stop_stream()
+            stream.close()
+        finally:
+            p.terminate()
