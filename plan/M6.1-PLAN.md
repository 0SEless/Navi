# M6.1 — Navigation Service Execution Plan

## Tasks

### T1. Add `travelTime` to Route

Add a `travelTime` computed property to the `Route` interface/class. Walking speed: 1.4 m/s.

**Files to touch:** `packages/runtime/src/routing/route.ts`

**Acceptance:** `Route` has `travelTime: { seconds: number; minutes: number; formatted: string }`

---

### T2. Create NavigationService

New class wrapping `RoutingEngine`. Methods: `findRoute`, `nearestNode`, `nearestEntrance`, `isReachable`. Uses domain result types, never exposes `NavNode`.

**Files to create:** `packages/runtime/src/engine/navigation-service.ts`

**Acceptance:** NavigationService constructed from `LoadedPackage`, exposes all 4 methods, `nearestNode` returns `{ nodeId: string; distance: number }`.

---

### T3. Wire NavigationService into RuntimeEngine

Replace `engine.routing` (RoutingAPI) with `engine.navigation` (NavigationService). Keep RoutingAPI/AStar/RoutingEngine exports from package index.

**Files to touch:** `packages/runtime/src/engine/runtime-engine.ts`, `packages/runtime/src/engine/index.ts`

**Acceptance:** `RuntimeEngine` has `navigation` property, no longer has `routing` property. Package still exports `RoutingEngine`, `AStar`, `RoutingAPI`.

---

### T4. Update package index

Export `NavigationService` from package index.

**Files to touch:** `packages/runtime/src/index.ts`, `packages/runtime/src/engine/index.ts`

**Acceptance:** `NavigationService` importable from `@navi/runtime`.

---

### T5. Write tests

Test all NavigationService methods: `findRoute`, `nearestNode`, `nearestEntrance`, `isReachable`, edge cases.

**Files to create:** `packages/runtime/src/engine/__tests__/navigation-service.test.ts`

**Acceptance:** 8+ tests covering all methods, edge cases (empty graph, nonexistent IDs, null route).

---

### T6. Verify full suite

Run all 103+ runtime tests and root-level vitest.

**Acceptance:** All tests pass, no type errors.
