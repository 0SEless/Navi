# M6.3 — Building Service Execution Plan

## Notes

- Purely additive — no existing BuildingAPI to replace or migrate
- `DataAPI.getBuildings()` and `DataAPI.getBuilding()` remain available for low-level access
- No changes to compiler, publisher, loader, or `LoadedPackage`

### Exports to add

| File | Change |
|------|--------|
| `src/engine/building-service.ts` | CREATE — new class + domain types |
| `src/engine/runtime-engine.ts` | Add `buildings` property |
| `src/engine/index.ts` | Export `BuildingService`, `BuildingResult`, `EntranceResult` |
| `src/index.ts` | Add `BuildingService` to exports |

---

## Tasks

### T1+T2. Create BuildingService with domain types

New class in `packages/runtime/src/engine/building-service.ts`. Methods: `get`, `list`, `findNearest`, `getEntrances`, `findContaining`. Domain types: `BuildingResult` (readonly, no `nodeId`), `EntranceResult` (readonly). Both sorted by name deterministically.

**Files to create:** `packages/runtime/src/engine/building-service.ts`

**Acceptance:** `BuildingService` constructed from `LoadedPackage`, exposes all 5 methods, `get()` returns `BuildingResult | undefined`, `getEntrances()` returns `EntranceResult[]` sorted by name, all methods handle `undefined` buildingIndex gracefully.

---

### T3. Wire BuildingService into RuntimeEngine

Add `buildings` property to `RuntimeEngine`. Import and construct from `LoadedPackage`. Update engine and package index exports.

**Files to touch:** `packages/runtime/src/engine/runtime-engine.ts`, `packages/runtime/src/engine/index.ts`, `packages/runtime/src/index.ts`

**Acceptance:** `RuntimeEngine` has `engine.buildings` as `BuildingService`. Package exports `BuildingService`, `BuildingResult`, `EntranceResult`.

---

### T4. Write BuildingService tests

Test all BuildingService methods: `get`, `list`, `findNearest`, `getEntrances`, `findContaining`, edge cases (empty buildingIndex, unknown ID, empty results).

**Files to create:** `packages/runtime/src/engine/__tests__/building-service.test.ts`

**Acceptance:** 10+ tests covering all methods, edge cases, immutability, deterministic ordering.

---

### T5. Verify full suite

Run all runtime tests.

**Acceptance:** All tests pass, no type errors.
