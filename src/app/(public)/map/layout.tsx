import { AdaptiveShell } from '@/components/public/AdaptiveShell'

export default function MapLayout({ children }: { children: React.ReactNode }) {
  return <AdaptiveShell>{children}</AdaptiveShell>
}
