# M6.5b — Panorama Service Execution Plan

## References to update

| File | Change |
|------|--------|
| `packages/runtime/src/engine/runtime-engine.ts` | Add `readonly panoramas: PanoramaService` |
| `packages/runtime/src/engine/index.ts` | Export `PanoramaService` |
| `packages/runtime/src/index.ts` | Export `PanoramaService` (if panorama types need exposure) |

No compiler / publisher / loader changes — M6.5a artifact stays frozen.

---

## Tasks

### T1. Create PanoramaService with domain types

New class in `packages/runtime/src/engine/panorama-service.ts`. Domain DTOs `PanoramaResult`, `HotspotResult`, `PanoramaPosition` defined in the same file (mirrors `SearchService`/`BuildingService`). Reads `pkg.panoramaIndex?.panoramas`. Implements `get`, `list`, `findNearest`, `getHotspots`, `resolve`. Uses a `distanceMeters` helper (same convention as `BuildingService`). Builds a `panoramaIds` set once for hotspot target resolution.

**Files to create:** `packages/runtime/src/engine/panorama-service.ts`

**Acceptance:** `PanoramaService` constructed from `LoadedPackage`; exposes all 5 methods; `get`/`list`/`getHotspots` project `PanoramaEntry`/`HotspotEntry` into DTOs; `findNearest` returns closest by straight-line distance; `resolve(roomId)` finds panorama with a `navigation` hotspot targeting `roomId`. No viewer imports.

---

### T2. Wire PanoramaService into RuntimeEngine

Add `readonly panoramas: PanoramaService` to `RuntimeEngine`, constructed in the constructor. Export from engine index + package index.

**Files to touch:** `packages/runtime/src/engine/runtime-engine.ts`, `packages/runtime/src/engine/index.ts`, `packages/runtime/src/index.ts`

**Acceptance:** `RuntimeEngine` has `engine.panoramas` as `PanoramaService`.

---

### T3. Write PanoramaService tests

Test all methods: `get` (found / missing), `list` (all / by building / empty), `findNearest` (closest / empty index), `getHotspots` (navigation→targetPanoramaId vs targetRoomId, link→targetUrl, unknown panorama), `resolve` (found / missing). Build a `LoadedPackage` with a `panoramaIndex` containing 2+ panoramas and a cross-link hotspot. Also test graceful absence (no `panoramaIndex`).

**Files to create:** `packages/runtime/src/engine/__tests__/panorama-service.test.ts`

**Acceptance:** 12+ tests covering all methods and edge cases; no viewer references.

---

### T4. Verify full suite

Run all runtime tests, then the full workspace suite.

**Acceptance:** All tests pass; no regressions.
