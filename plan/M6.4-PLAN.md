# M6.4 — Location Service Execution Plan

## References to update

| File | Change |
|------|--------|
| `src/engine/position-api.ts` | DELETE — replaced by location-service.ts |
| `src/engine/runtime-engine.ts` | Replace `PositionAPI` with `LocationService` |
| `src/engine/index.ts` | Replace `PositionAPI` export with `LocationService` |
| `src/index.ts` | Replace `PositionAPI` in export line with `LocationService` |
| `src/engine/__tests__/runtime-engine.test.ts` | `engine.position.getCurrentFloor()` → `engine.location.snapToNode({...}).node.floor` |

Files to keep (backward compat): `PositionEngine`, `GpsResolver` remain exportable from `src/position/`.

---

## Tasks

### T1+T2. Create LocationService with domain types + resolveContext

New class in `packages/runtime/src/engine/location-service.ts`. Primary method `resolve(position)` backed by private `resolveContext()`. Convenience projections: `snapToNode`, `getBuilding`, `getFloor`, `isInsideBuilding`. Domain types: `SnapResult`, `LocationContext`. Uses `NavNode` directly as the snapped node type.

**Files to create:** `packages/runtime/src/engine/location-service.ts`

**Acceptance:** `LocationService` constructed from `LoadedPackage`, exposes all 5 methods, `resolve()` computes everything in one pass, convenience methods are projections over `resolve()`.

---

### T3. Wire LocationService into RuntimeEngine + delete PositionAPI

Replace `PositionAPI` with `LocationService` in RuntimeEngine. Update engine and package index exports. Delete `position-api.ts`. Update the single test reference.

**Files to touch:** `packages/runtime/src/engine/runtime-engine.ts`, `packages/runtime/src/engine/index.ts`, `packages/runtime/src/index.ts`, `packages/runtime/src/engine/__tests__/runtime-engine.test.ts`

**Files to delete:** `packages/runtime/src/engine/position-api.ts`

**Acceptance:** `RuntimeEngine` has `engine.location` as `LocationService`. `PositionAPI` no longer exists. Package still exports `PositionEngine`, `GpsResolver`.

---

### T4. Write LocationService tests

Test all LocationService methods: `resolve`, `snapToNode`, `getBuilding`, `getFloor`, `isInsideBuilding`, edge cases (empty graph, missing buildingIndex, far position).

**Files to create:** `packages/runtime/src/engine/__tests__/location-service.test.ts`

**Acceptance:** 10+ tests covering all methods, stateless behavior, `resolveContext` sharing, immutability.

---

### T5. Verify full suite

Run all runtime tests.

**Acceptance:** All tests pass.
